// We're using direct imports in loading-state.tsx and loading-demo-page.tsx instead of this index file
// This is being kept as a reference and may be useful in the future

/*
import { HeartbeatLoader } from './heartbeat-loader';
import { StethoscopeLoader } from './stethoscope-loader';
import { PulseLineLoader } from './pulse-line-loader';
import { PillLoader } from './pill-loader';
import { MedicalCrossLoader } from './medical-cross-loader';

export {
  HeartbeatLoader,
  StethoscopeLoader,
  PulseLineLoader,
  PillLoader,
  MedicalCrossLoader
};
*/